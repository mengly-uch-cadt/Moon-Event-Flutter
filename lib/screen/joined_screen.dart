import 'package:flutter/material.dart';

class MoonJoinedScreen extends StatelessWidget {
  const MoonJoinedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Joined Screen"),
    );
  }
}